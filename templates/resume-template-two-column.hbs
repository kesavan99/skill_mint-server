<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{personalInfo.name}} - Resume</title>
  <style>
    /* Reset for this template only */
    #resume-two-column * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    #resume-two-column {
      font-family: 'Arial', sans-serif;
      line-height: 1.5;
      color: #333;
      background: #fff;
      font-size: 10.5pt;
      width: 100%;
      max-width: 800px;
      margin: 0 auto;
    }

    #resume-two-column .container {
      width: 100%;
      max-width: 800px;
      margin: 0;
      padding: 0;
      display: flex;
      min-height: 100vh;
      flex-direction: row;
      flex-wrap: nowrap;
    }

    /* Left Column - Sidebar */
    #resume-two-column .sidebar {
      width: 280px;
      min-width: 280px;
      max-width: 280px;
      background: #2c5f7c;
      color: #fff;
      padding: 40px 30px;
      flex-shrink: 0;
      flex-grow: 0;
    }

    #resume-two-column .profile-section {
      margin-bottom: 35px;
    }

    #resume-two-column .profile-section h1 {
      font-size: 22pt;
      margin-bottom: 5px;
      font-weight: 600;
      line-height: 1.2;
    }

    #resume-two-column .contact-info {
      margin-top: 20px;
    }

    #resume-two-column .contact-item {
      margin-bottom: 12px;
      font-size: 9.5pt;
      line-height: 1.4;
    }

    #resume-two-column .contact-item a {
      color: #fff;
      text-decoration: none;
    }

    #resume-two-column .sidebar-section {
      margin-bottom: 30px;
    }

    #resume-two-column .sidebar-title {
      font-size: 12pt;
      font-weight: 600;
      margin-bottom: 15px;
      padding-bottom: 8px;
      border-bottom: 2px solid #3a7ca5;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    #resume-two-column .skill-item {
      margin-bottom: 10px;
      font-size: 9.5pt;
      padding-left: 12px;
      position: relative;
    }

    #resume-two-column .skill-item:before {
      content: "▸";
      position: absolute;
      left: 0;
      color: #3a7ca5;
    }

    /* Right Column - Main Content */
    #resume-two-column .main-content {
      flex: 1;
      flex-grow: 1;
      flex-shrink: 1;
      padding: 40px 40px 40px 35px;
      background: #fff;
    }

    #resume-two-column .section {
      margin-bottom: 30px;
    }

    #resume-two-column .section-title {
      font-size: 13pt;
      color: #2c5f7c;
      font-weight: 600;
      margin-bottom: 15px;
      padding-bottom: 8px;
      border-bottom: 2px solid #2c5f7c;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    /* Summary */
    #resume-two-column .summary {
      color: #555;
      font-size: 10pt;
      line-height: 1.6;
      margin-bottom: 15px;
      text-align: justify;
    }

    /* Experience */
    #resume-two-column .experience-item {
      margin-bottom: 20px;
    }

    #resume-two-column .experience-header {
      display: flex;
      justify-content: space-between;
      margin-bottom: 3px;
    }

    #resume-two-column .job-title {
      font-weight: 600;
      font-size: 11pt;
      color: #2c5f7c;
    }

    #resume-two-column .duration {
      font-size: 9.5pt;
      color: #666;
      font-style: italic;
    }

    #resume-two-column .company {
      font-size: 10pt;
      color: #666;
      margin-bottom: 8px;
      font-style: italic;
    }

    #resume-two-column .experience-item ul {
      margin-left: 20px;
      margin-top: 6px;
    }

    #resume-two-column .experience-item li {
      font-size: 10pt;
      line-height: 1.5;
      margin-bottom: 5px;
      color: #555;
    }

    /* Education */
    #resume-two-column .education-item {
      margin-bottom: 15px;
    }

    #resume-two-column .education-header {
      display: flex;
      justify-content: space-between;
      margin-bottom: 3px;
    }

    #resume-two-column .institution {
      font-weight: 600;
      font-size: 10.5pt;
      color: #2c5f7c;
    }

    #resume-two-column .year {
      font-size: 9.5pt;
      color: #666;
    }

    #resume-two-column .degree {
      font-size: 10pt;
      color: #666;
    }

    /* Projects */
    #resume-two-column .project-item {
      margin-bottom: 18px;
    }

    #resume-two-column .project-name {
      font-weight: 600;
      font-size: 10.5pt;
      color: #2c5f7c;
      margin-bottom: 5px;
    }

    #resume-two-column .project-item ul {
      margin-left: 20px;
      margin-top: 5px;
    }

    #resume-two-column .project-item li {
      font-size: 10pt;
      line-height: 1.5;
      margin-bottom: 4px;
      color: #555;
    }

    /* Certifications */
    #resume-two-column .cert-list {
      list-style: none;
      margin-left: 0;
    }

    #resume-two-column .cert-list li {
      font-size: 10pt;
      margin-bottom: 8px;
      color: #fff;
      padding-left: 12px;
      position: relative;
    }

    #resume-two-column .cert-list li:before {
      content: "✓";
      position: absolute;
      left: 0;
      color: #3a7ca5;
      font-weight: bold;
    }

    @media print {
      .page-break {
        page-break-before: always;
      }
    }
  </style>
</head>
<body>
  <div id="resume-two-column">
    <div class="container">
      <!-- Left Sidebar -->
      <div class="sidebar">
      <!-- Profile -->
      <div class="profile-section">
        <h1>{{personalInfo.name}}</h1>
        <div class="contact-info">
          <div class="contact-item">📧 {{personalInfo.email}}</div>
          <div class="contact-item">📱 {{personalInfo.phone}}</div>
          {{#if personalInfo.location}}
          <div class="contact-item">📍 {{personalInfo.location}}</div>
          {{/if}}
          {{#if personalInfo.linkedin}}
          <div class="contact-item">🔗 <a href="{{personalInfo.linkedin}}">LinkedIn</a></div>
          {{/if}}
        </div>
      </div>

      <!-- Skills -->
      {{#if skills}}
      <div class="sidebar-section">
        <h2 class="sidebar-title">Skills</h2>
        {{#each skills}}
        <div class="skill-item">{{this}}</div>
        {{/each}}
      </div>
      {{/if}}

      <!-- Certifications -->
      {{#if certifications}}
      <div class="sidebar-section">
        <h2 class="sidebar-title">Certifications</h2>
        <ul class="cert-list">
          {{#each certifications}}
          <li>{{this}}</li>
          {{/each}}
        </ul>
      </div>
      {{/if}}
    </div>

    <!-- Main Content -->
    <div class="main-content">
      <!-- Summary -->
      {{#if summary}}
      <div class="section">
        <h2 class="section-title">Professional Summary</h2>
        <p class="summary">{{summary}}</p>
      </div>
      {{/if}}

      <!-- Experience -->
      {{#if experience}}
      <div class="section">
        <h2 class="section-title">Work Experience</h2>
        {{#each experience}}
        <div class="experience-item">
          <div class="experience-header">
            <span class="job-title">{{title}}</span>
            <span class="duration">{{duration}}</span>
          </div>
          <div class="company">{{company}}</div>
          <ul>
            <li>{{description}}</li>
          </ul>
        </div>
        {{/each}}
      </div>
      {{/if}}

      <!-- Education -->
      {{#if education}}
      <div class="section">
        <h2 class="section-title">Education</h2>
        {{#each education}}
        <div class="education-item">
          <div class="education-header">
            <span class="institution">{{institution}}</span>
            <span class="year">{{year}}</span>
          </div>
          <div class="degree">{{degree}}</div>
        </div>
        {{/each}}
      </div>
      {{/if}}

      <!-- Projects -->
      {{#if projects}}
      <div class="section">
        <h2 class="section-title">Projects</h2>
        {{#each projects}}
        <div class="project-item">
          <div class="project-name">{{name}}</div>
          <ul>
            <li>{{description}}</li>
            {{#if technologies}}
            <li>Tech Stack: {{technologies}}</li>
            {{/if}}
          </ul>
        </div>
        {{/each}}
      </div>
      {{/if}}
    </div>
  </div>
</body>
</html>
